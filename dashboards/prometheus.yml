global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: "mistral_rag_agent"
    static_configs:
      - targets: ["10.128.0.3:8001"]
    metrics_path: /metrics
